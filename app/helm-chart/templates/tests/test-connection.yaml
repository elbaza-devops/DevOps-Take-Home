apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-world-js.fullname" . }}-test-connection"
  labels:
    {{- include "hello-world-js.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-world-js.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
